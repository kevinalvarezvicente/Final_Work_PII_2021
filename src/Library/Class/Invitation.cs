using Library;
using PII_Proyecto_Final_TEMP.src.Library.Class;

namespace Library
{
    public class Invitation
    {
        public void generateInvitation()
        {
            Token ClassToken = new Token();
            ClassToken.createToken(1);
            ClassToken.createToken(2);
            ClassToken.createToken(3);

        }
    }
}